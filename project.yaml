name: graven
version: 1.0.3-DEV
go_version: ">=1.12.0"
artifacts:
- classifier: darwin
  targets:
  - executable: bin/graven
    package: .
  archive: tgz
  env:
    GOARCH: amd64
    GOOS: darwin
- classifier: linux
  targets:
  - executable: bin/graven
    package: .
  archive: tar.gz
  env:
    GOARCH: amd64
    GOOS: linux
- classifier: win
  targets:
  - executable: graven.exe
    package: .
  archive: zip
  env:
    GOARCH: amd64
    GOOS: windows
repositories:
  github:
    group: cbegin
    artifact: graven
    type: github
    roles:
    - release
resources:
- LICENSE
